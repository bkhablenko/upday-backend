version: '3'
services:
  postgres:
    image: postgres:15.2-alpine
    environment:
      POSTGRES_DB: upday
      POSTGRES_USER: root
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
